# Legacy compatibility override.
# Run with:
#   docker compose --env-file .env -f docker-compose.yml -f docker-compose.internal.yml up -d
# The production services stay defined in docker-compose.yml so this file cannot drift.

services:
  backend:
    env_file:
      - ./backend/.env.internal

  ai-worker:
    env_file:
      - ./backend/.env.internal
