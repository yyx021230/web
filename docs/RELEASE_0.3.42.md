# v0.3.42 发布预检修复

- 包含 v0.3.41 的全部业务改动与上线注意事项，详见 RELEASE_0.3.41.md。
- v0.3.41 首次上线预检发现 Windows PowerShell 对空管道的字符串转换仍可能返回 null；此前未创建去水印 Worker，调用 Trim 失败。失败发生在构建与停服之前，未切换生产版本。
- 改用字符串插值处理无容器的正常情况；发布失败日志保留原始脚本位置和调用栈。
- 不修改原有发布标签；使用新版本与新提交发布，保留审计链。
