# 精准单篇：图片版式重新分类

## 状态与核对范围

本地分类、预览和测试已修改；**尚未上传或重启 Windows 线上服务**，没有新建生图任务。
数据来自 2026-09-08 15:24 的线上提示词目录，共 525 条；没有下载成图或改写原始提示词库。

本次做的是全量提示词程序复分、34 条真实原文回归样本、线上现有误分类型的原图抽查，
不等于宣称 525 张图片全部经过人工视觉审批。批量默认候选池与生成/OCR校验未修改。

## 确认的错误

| 原案例 | 旧分类 | 实际依据与新分类 |
| --- | --- | --- |
| #335 | 手账便签风 | 原文取消便签，原图为四张独立配置报价卡 → 卡片报价单 |
| #353、#379 | 参数信息图 | 原文只有车、标题与庭院背景，没有参数块；#353 原图核对一致 → 场景主视觉 |
| #535、#477 | 多图拼贴 | “粉白拼接”说的是字体，只有一个汽车风景画面；#477 原图核对一致 → 场景主视觉 |
| #438、#186 | 多图拼贴 | “拼接”修饰地坪、建筑，不是多个照片 → 场景主视觉 |
| #523 | 多配置报价单 | “配置对比”只出现在辅助话术，没有报价行列 → 棚拍主视觉 |
| #574、#576 | 大字标题海报 | 普通车型标题不应自动算巨字创意；原图核对后分别为棚拍、场景主视觉 |
| #407 | 多配置报价单 | “表格风”不等于实际表头；内容承载是报价卡/分块 → 卡片报价单 |
| #332 | 未正确独立识别 | 参数、全系标配、版本差异组成购车手册 → 参数配置图 |

旧规则问题：关键词命中即停止；否定、原图对比、背景材质、营销话术混入结构判断。
另有只修预览的 #335 特判，把它改成棚拍但没有修生产端；本次撤掉这条不一致的修正。

## 新分类（9 类）

| 类型 | 边界 | 原库归类数 | 现有 Worker 过滤后数量 |
| --- | --- | ---: | ---: |
| 场景主视觉海报 | 实景中的汽车主体，品牌/常规标题辅助 | 207 | 157 |
| 棚拍主视觉海报 | 纯色、渐变或设计背景中的单车主视觉 | 190 | 155 |
| 巨字创意海报 | 巨字、字效、叠压本身参与构图 | 5 | 3 |
| 价格权益大字 | 一组主价格/权益金额成为视觉中心 | 24 | 20 |
| 表格报价单 | 实际配置报价表头、行列、单元格 | 42 | 11 |
| 卡片报价单 | 多个配置分卡/分块报价，没有共用规整表头 | 17 | 5 |
| 多图拼贴 | 多个独立照片/细节画面 | 1 | 0 |
| 手账便签风 | 整张画面由纸张、胶带、便签组织 | 1（批准的 #324/方案10） | 1 |
| 参数配置图 | 参数标注、配置图表、购车手册 | 1 | 1 |

数字为未去重目录的静态核对，不等于网页去重后的展示数量，也不是生产成功数量。
原目录 525 条另加 1 个已批准的分段视图，共 526 个核对对象；38 个对象暂不硬归类，
包括混合完整方案、聊天复合版式、其他信息表、信息不足的编辑指令等。它们没有被删除，
默认批量素材池未因此收缩。没有证据的类型不会拿错图填满。

## 已做的本地调整

- 共用 `image_layout_analysis` 判断真实版式，并返回归类理由和风格标签。
- 表格报价与卡片报价分开；“多个配置需完整价格”的原有要求继续保留。
- 手账 #324/方案10 的抽取移到共享函数，预览与 Worker 使用同一段原文与指纹。
  不再把不对应方案10的整条母图显示成手账样图。
- 忽略过期的 `preview_type` 特判，避免预览和生产标签不同。
- 页面保留现有风格，新增两种结构示意；每例可展开“归类依据”。
  数字标明是“归类参考”，不暗示全部能通过生产校验。零母版选项不能下发。
- 切换车型后重新检查报价卡的资料要求，前端与 API 均保留价格条件限制。
- 保留旧类型名称兼容、默认池不变、原始文字不改、报价与文案类型独立选择。

## 另行确认后才能改的生产限制

1. **#568 的文字槽位被误算。** 现有 `source_slot_count` 算到 11 个（上限 9），其中包括
   “整体配色”“大饼”“价格信息-可选择性添加在底部”“整体风格”等描述/章节名，
   并把重复 D19 再次计算。这与真正需要画出来的文字块不是同一概念。
   因此真实多图拼贴虽然归类正确，现有安全候选筛选仍返回 0。
   修复应只纠正槽位识别，不笼统扩大文字上限、不降低金额/配置/OCR要求。
2. **单篇默认要求主母版+备用母版共 2 条。** 参数配置图当前只剩 1 条真实候选，仍会报不足。
   当前只有已批准的 #324 分段享有单母版例外；其他类型没有。
   如批准，可在精准单篇只有一条合格母版时重复使用同一母版重试，不跨类型找备用。
3. 部分报价母版的原有生产识别对英文版本、带空格配置名覆盖不足，归类数与 Worker
   数量也受文字长度、文字位、无字图要求等原有规则影响。本次没有顺带放宽或重写这些规则。

以上均为只读静态复现，未为了验证而产生收费生成任务。需要用户确认范围后继续生产适配及上线。

## 回归与证据位置

- 生产选择/核心规划/延迟相关回归：152 项通过。
- Web API 分类、权限、快照、政策门槛及工作流回归：36 项通过。
- 前端工作流组件与 API 客户端：41 项通过；TypeScript 检查通过。
- 合计 229 项，属于代码回归，不代表新分类已完成真实生图验收。
- 原目录快照：`artifacts/hermes-image-types-20260908/before/prompts.json`。
- 新分类逐条理由、目录计数、Worker 静态筛选：`artifacts/hermes-image-types-20260908/after/classification.json`。
- 原文回归样本：`backend/tests/fixtures/hermes_image_layouts.json`。
