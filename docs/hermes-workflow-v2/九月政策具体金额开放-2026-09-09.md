# 九月政策具体金额开放：Worker 0.3.24

用户先明确要求具体金额也用于生成内容，听取影响说明后确认“确定覆盖”。据此覆盖9月报告中的地方补贴具体金额不公开规则，不再仅恢复页面展示。

## 已完成

- 11款车型、58个配置的 `provincial_trade_in_after_price` 恢复为原表实际数值，页面、文案政策输入、报价图片登记共用一份数据。例如A05 405舒享版恢复为 ¥58,788，口径为按置换更新条件测算后的车价，不是落地价。
- 解析器读取 `policy_overrides.json` 的 `_publication.allow_local_subsidy_amounts=true`。授权人为用户、确认日期2026-09-09、适用期2026-09均有记录；同月刷新会保留，不跨月自动沿用本次覆盖。
- 文案提示词依据实际开关允许对应金额；图片提示词允许原有省补/置换价格槽引用同配置登记值，不扩大母版结构或强塞政策。
- 地方补贴金额隐藏拦截关闭，其余显式禁词、厂家综合权益上限、互斥条件、新车口径、金额/日期/配置回查保持。将置换补贴后车价加入原有“不得冒充落地价”检查。
- 原文不修改，原表金额不自行重算；没有恢复任何原表不存在的城市加码金额，不允许擅自叠加到厂家综合权益。

## 部署与验证

- Windows `192.168.10.107`，Worker从0.3.23升级到0.3.24，2026-09-09 15:21重载并健康。
- 前端、Hermes API保持0.3.22，无需重启：API直接读取共享政策文件，因此网站版本号仍显示0.3.22。
- 生产侧397项、接口38项，共435项回归通过。
- 候选Linux镜像对58个配置分别执行文案、图片规划、OCR静态检查，共174组通过；未调用模型或生图接口。
- 活跃Worker在临时目录真实下载原文并重新解析，结果与部署文件一致，未改生产缓存。新政策 SHA256：`1313390f3d30568d5551206034ca923a162a2c388d092182a5404f903e8abb37`。
- 上线前后API直接加载的政策与部署文件一致；文案库、提示词库、任务、帖子和定时设置旧记录指纹均未改变，未额外生图、启用定时或发布。
- Chrome预览刷新遇到客户端拦截（ERR_BLOCKED_BY_CLIENT），没有绕过或修改浏览器安全设置。API数据核验和局域网HTTP 200通过，但公网入口另有连接重置问题：Mac直连curl为52/空响应，Windows为56/连接重置；没有把公网或浏览器端标为验收通过。网站/API容器身份及启动时间不变，没有擅自改网络配置或重启转发服务。

## 回退与未处理事项

备份仅保留在Windows：

- 政策/缓存：`D:\ztqc-hermes-release\0.3.24\private\policy-before-20260909T072140Z`
- Compose/版本记录：`D:\ztqc-hermes-release\0.3.24\private\service-before-20260909_152139`
- 发布证据：`D:\ztqc-hermes-release\0.3.24`，当前记录：`C:\projects\web\.release\hermes\current.json`。

回退前检查后续任务和政策修改，等待Worker完成任务，恢复上述政策/缓存及Worker0.3.23，不回滚数据库或历史帖子。前述Lafa5 Ultra空格误判、母图槽位不等价替换问题仍保留为未处理，不得写成此版本已解决。

本次没有新图文生产验收，静态检查通过不等于保证任何未来生成结果100%合格。
