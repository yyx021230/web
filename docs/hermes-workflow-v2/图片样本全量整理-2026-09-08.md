# 图片样本全量整理 · 0.3.22

2026-09-08 上线。范围是用户确认的 cleaned-final 108 张历史代表图，以及原线上 525 条有效提示词；不改历史图文、不生成新图、不启用定时或发布。

## 已交付

- 公网：<http://47.98.127.132:18080/workflows/hermes/single>
- Windows：<http://192.168.10.107:3000/workflows/hermes/single>
- 106 条新增，2 条精确匹配既有原文并复用，共 631 条有效素材。
- 工作流图片统一为 8 类：单图主视觉、大字主视觉、多图拼贴、表格对照、分块卡片、清单笔记、对话问答、图解科普。
- 旧库通过 22 页联系表逐页核对版式；新旧素材使用同一个按原文指纹维护的分类/版式族注册表。联系表核对不代表历史金额、配置或政策已核实。
- 确认同版的变体只保留一个展示/选材入口，原始素材不删除、不改写。631 条汇总为 572 个版式入口，571 个可浏览案例；旧 #14 原图不可用，未进入生产候选。
- 用户指出的 #8652/#8651/#8646/#8671 归为一族，保留 #8652；#15691/#15690/#15689/#15686 归为一族，保留 #15691。
- 不再只展示每类前三张。图片案例每页 6 张，可浏览全部；来源任务号、原始提示词和原图均可查。
- 旧类型 ID/名称继续兼容历史任务；新下发的 8 类使用统一规则。批量不指定类型时同样按确认的版式族去重。

## 实际生产路径核验（未生成内容）

在运行中的 Worker 使用正常 `OnlineData.prompts()` 读取真实线上 API，读到 631 条，108 张批准样本全部在内。调用实际安全候选池及类型筛选函数，逐组数量与 API 页面返回完全一致，候选池内没有重复版式族。

| 图片类型 | 可浏览案例 | 有完整报价资料的静态候选 | 无完整报价资料的静态候选 |
| --- | ---: | ---: | ---: |
| 单图主视觉 | 372 | 291 | 284 |
| 大字主视觉 | 46 | 34 | 33 |
| 多图拼贴 | 13 | 9 | 8 |
| 表格对照 | 65 | 26 | 0 |
| 分块卡片 | 46 | 27 | 3 |
| 清单笔记 | 13 | 4 | 2 |
| 对话问答 | 5 | 1 | 1 |
| 图解科普 | 11 | 8 | 3 |
| 合计 | 571 | 400 | 334 |

以上是原提示词结构、文字槽位和报价资料条件的静态筛选，不是生成质量通过数。当前数据下对话类只剩 1 个可生产候选，没有为了凑数量放宽槽位/金额/配置/OCR 校验。每个输入车型仍需检查自己的资料，不能因为“表格/卡片”属于某大类就绕过多配置报价条件。

保留标准文字槽位 2–9、报价槽位 2–30 和既有文字长度/无字图/多车型报价约束。仅对经过指纹核对的真实聊天、清单、图解版式补充格式识别，不再因为其原文缺少“海报/封面”两个词而错误排除。

## 验证证据

- 本地回归：生产 361、API/导入/工作流 38、生命周期/安全等 27、前端 61，共 487 项通过；TypeScript 检查通过。
- 候选 Worker 镜像断网校验全部 108 张的分类与静态准入；完整资料条件下 8 类都有候选。
- 导入前核对全部原生图任务已完成、原文逐字相同、图片路径确在该任务结果中；一次事务写入并记录来源审计。
- 导入后再次只读预检：新增 0、复用 108，证明可重复运行而不重复导入。
- 导入前后逐行哈希：既有提示词、工作流任务、帖子和定时配置均未变化；政策文件哈希不变。
- 线上任务仍为 3 个待审核，定时任务仍关闭。本次生产请求为 0。
- 实际浏览器检查公网和 Windows：版本 0.3.22、8 类入口、原图加载、分页换例、历史任务可见、缺报价资料时禁用无候选类型。
- 公网原图体积较大，首次加载比缓存慢；没有压缩或改写原图。本次没有宣称完成 40 篇吞吐量或端到端成图质量验收。

## 部署与回退

仅替换 `frontend`、`hermes-frontend`、`hermes-api`、`hermes-worker` 四个容器；数据库、Redis、MinIO、核心后端和原生图 Worker 的容器 ID、镜像、启动时间均不变，FRP 配置和进程未改。

- 镜像版本：0.3.22；构建标识：`catalog-183b4fda`（定向源码覆盖包标识，不是 Git 提交）。
- 注册表 SHA256：`f5f0e3a1db409140d45ee04014a8b67ec769f6cbd2b72439b36c78151ac7ca7f`。
- 导入清单 SHA256：`daac0210f52d70d219f5db36c5e73b7d41d886f63068fd0cad4f58f577431448`。
- Windows 发布目录：`D:\ztqc-hermes-release\0.3.22`。
- Windows 配置备份：`D:\ztqc-hermes-release\0.3.22\private\before_20260908_185021`。
- 证据：`catalog-before.json`、`catalog-import.json`、`catalog-after.json`、`catalog-repeat-dry-run.json`、`catalog-worker.json`、`release-receipt.json`，均保存在上述 Windows 发布目录。
- 当前发布记录：`C:\projects\web\.release\hermes\current.json`。
- 原 0.3.21 镜像保留；必要时先排空 Worker，再恢复四个工作流镜像/覆盖配置。新增样本是增量数据，不应恢复整个数据库；若退回旧分类器，需核对新增 ID 的后续引用后再决定是否暂停其选材入口。
- 核心后端实际运行 `ztqc/backend:0.3.17`，旧核心发布记录仍写 0.3.18；本次不改其版本、不处理这项独立恢复造成的元数据差异，只记录实际镜像。
- 历史成图已报告的内容问题继续保留在发布记录中，本次分类整理不将其标记为解决。

本地和 Windows 的定向脚本见 `scripts/windows/*HermesCatalog*`、`prepare_hermes_catalog.py`、`import_hermes_catalog.py`、`probe_hermes_catalog*.py`；共享规则位于 `backend/app/services/hermes_reference_types.py` 与 `backend/app/data/hermes_image_curation.json`。以后更改原文会失去原指纹的人工核对标记，需要重新归类，不能沿用旧结论。
