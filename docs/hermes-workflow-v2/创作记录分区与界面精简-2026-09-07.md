# 创作记录分区与界面精简 · 2026-09-07

## 范围

按用户指定的 https://www.meigen.ai/skills 学习浅色表面、克制的文字层级和卡片排版。保留现有网站导航、表单下发、逐篇回传、编辑记录、审核与原文对照。不修改生产提示词、Worker、政策、已有图文、定时或发布。

## 历史归属

- 两个列表接口新增 `workflow_mode=batch|single`，在数据库计数和分页前过滤。
- `manual` 属于单篇；`manual_batch`、`schedule`、`admin_manual` 属于批量。按入口，不按篇数或是否指定类型。
- 重新生成跟随最初任务的归属，旧记录通过递归查询其来源解析，不重写历史。不合法或断开的来源链默认归批量；只沿递增任务 ID 查询，避免循环。
- 列表及详情返回 `workflow_mode`。跨模块的旧链接显示跳转入口，不将另一模块任务插入当前记录。
- 管理员未指定模式时仍看全部；筛选、搜索、刷新、分页保持模块参数。

## 视觉与交互

- 移除 1400px 的容器宽度上限；采用小边距、浅底大表面和下划线导航。
- 新建区域减少重复标题、说明与提示；保留必要的事实限制，账号历史规则收进悬浮说明。
- 批量按任务及账号分组排列帖子；单篇采用并列的独立任务卡片，不再一篇占整行。
- 配图完整显示，不裁切图内文字。全文及话题可展开、可复制；审核与编辑不变。
- 小屏回到单列，保留页面纵向滚动、可访问的按钮名称与焦点样式。

## 回归验证

覆盖单篇与 1 篇批量的区别、连续两层旧重生归属、分页总数、非法模式、交叉筛选、旧链接不串入另一模块，以及逐篇回传、编辑、拒绝重生和全文话题保留。

API 需重新加载后才能使用新的分类参数。前端使用开发服务热更新；无需重启生产 Worker，不创建测试生图任务。

## 实际落地验收

2026-09-07 已完整同步到 `/Users/yyx/ztqc/web` 并在队列空闲时重新加载本地 API（端口 8000，lifespan off）。不再只是工作树里的代码或样式预览；Worker 未重启。

- 后端 30 项测试、前端 27 项测试通过，包含分页/搜索/刷新保留分区，以及切换模块后忽略较晚返回的旧响应。
- 浏览器连续切换 3 轮：批量显示任务 10、9、3、1；单篇显示任务 8、7、6、5、4、2。没有跨区混入。
- 单篇“已通过”筛选只剩任务 8；清除筛选及刷新后恢复 6 个单篇任务。
- 1470px 浏览器窗口测得 4 列，小窗口测得 2 列，均无横向溢出。
- 实测展开完整正文及话题、打开编辑和不通过面板，不提交保存或审核。
- 对全部帖子、任务参数、状态及定时配置的只读哈希比对一致，没有新增生图或改动已有内容。

完整应用前备份：`/private/tmp/hermes-history-full-before-20260907.Uv8o6T`。此前只有 3 个视觉文件的预览备份：`/private/tmp/hermes-style-preview-before-20260907.N8OS7q`。
