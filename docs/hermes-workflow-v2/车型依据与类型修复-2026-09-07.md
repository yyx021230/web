# 车型依据与类型修复 · 2026-09-07

## 授权与范围

用户已同意修复上次真实生产发现的图片分类误判、金额/配置名误拦、无依据试驾体验，并允许从已有 Dify 车型知识库导入缺失产品资料。第一篇不改、旧结果不覆盖、不发布、不启用定时。当前报告是修复记录，不能替代新一轮真实生成验收。

## 已实现

1. 图片类型 v2：单独“手写”“毛笔”“小贴纸”“手绘图标”不再决定手账类型；要求纸张、便签、手账主体的明确布局/风格依据。默认随机模式保持原完整候选池，指定类型缺素材时仍不偷偷换类别。没有删除线上任何模板。
2. 文案金额比较以元为单位归一，6.39w / 6.39万元 / ¥63,900 可等值；不把积分当现金。配置校验只取配置实体，不包含“优先看”等句子前缀，入门版/高配版/低配版等泛称不当作登记配置；未知具体版本和金额仍报错。报价结构检测原有宽匹配保持不变。
3. 现有 `xhs_get_copy_case` 接入 `product_knowledge`，不新增一次模型调用。案例政策保持原文，产品资料独立返回；每个生产批次冻结资料快照，文案产出记录留存检索结果及来源。
4. 复刻提示词明确：母车的噪音、悬架、空间体验不能直接移植；参数不能当亲测证据。没有依据时删除无依据结论或改成试驾观察问题，必要时把“试驾完”改成“试驾前”；不能为了复刻句式虚构亲历，也不能强塞知识库参数。

## Dify 导入证据

- 服务器：8.163.58.214；只读查询，未更新、索引或重启 Dify。
- 知识库：汽车产品信息，ID `745f1046-8234-4349-b785-1893dbed6bed`。
- 使用当前启用且索引完成的文档 `零跑汽车_官网当前在售_20260819_v2.md`，ID `2d77822c-8e24-40bc-9cc2-ffee65cf9e62`。
- 原样导入 62 个分段，保留顺序、原文、分段 ID、状态、导出时间及 SHA256。解析出 15 个车型/动力分组、51 个完整年款版本；少数分段跨版本或包含截断标题，按显式品牌/车型/版本标题恢复归属，缺分段不跨空档拼接，年款后没有版本的截断标题不当独立车型。
- 产品读取：同品牌、车型、年款和版本严格对应；纯电/增程分开，六座/6座等字面形式归一。不同配置的专有功能不作为全系共有；缺版本时不输出“全系共有”事实。
- 检索给 Agent 的产品字段排除售价、补贴、金融、质保等政策信息、未知占位值；这些仍以用户当前政策为准。原始快照不改造，价格只在检索视图中隔离。
- 产品片段按母文已有技术话题选取，没有参数槽的母文不因知识库资料多而加参数。快照超 30 天会标记待刷新，不作为新一轮产品补充事实。

## 不能假装已经补齐的资料

- A05：只在停用的历史文档中命中旧“26款 标准版”，与现政策不一致。当前有效文档无 A05，返回 missing；现政策已有概览仍可用，但没有真实试驾证据。
- C10：政策明确写 2026 款，有效知识库写 2027 款，不能自动跨年款借参数。
- D99：现政策的“领航版”在该快照没有对应版本；只读取其他已匹配版本，不冒充全配置覆盖。
- 未修改用户政策、未重新启用历史文档、未用车型库中的图片推断参数。

## 重测前的素材阻塞

对线上 525 条提示词完整检查：没有可直接使用的独立“手账便签风”母版。#54 是建筑光影加毛笔字，#260 是旅行摄影加手绘图标，均不能继续当作手账。#324 的“方案10”确实有白色桌面、胶带、拍立得、手撕便签、彩色便利贴，但这一条混合了 3D / 山水 / 手账三套提示词，且包含多配置报价内容，不能原封不动送入生成。

用户随后明确同意提取 #324 的方案10并重测。实现为运行时只读视图，仅对指定手账类型生效，原 ID / 全文 / SHA256 / 子方案标题均保留；核对全文哈希与边界，原文变动后不猜测提取。只把四张便签价格行分行，文字内容保持原样供 Agent 按当前政策最小替换。原记录配图未确认属于方案10，因此不把它冒充该子方案的示例图。

这张母版同时具有手账视觉与多配置报价内容，仍按报价硬约束处理（只允许有完整配置报价的政策），不借此将正文强行变成报价文章。仅在单篇且只有这一条已批准子方案时允许没有备用母版，原有同母版生图重试仍保留；不借用其他类型来凑备用。普通任务的候选和备用策略不变。

通过用户已登录 Web 页面创建任务 #7，账号 42（云先派懂车社）、A05、试驾测评 × 手账便签；没有改写 #5/#6。运行结束前，此处不能视为生成验收通过。

## 验证与边界

- Worker/core/知识检索、子方案提取及图片返回结构回归 65 项；隔离 SQLite 的 Web API 回归 17 项，共 82 项。
- 覆盖：金额等值与未知金额、积分与现金隔离、配置前缀/具体错误版本、字体与版式区分、车型/年款/动力/座数对应、跨块原文来源、停用文档排除、局部版本覆盖与无政策价格污染。
- 自然语言事实核对目前由明确生成规则和有来源的上下文约束，不宣称代码已能穷尽判断所有语义体验错误。仍需真实单篇重测后人工对照成图和正文；不会把单元测试通过写成生成效果已达标。
- 离线重放 #6 保存的四次文案校验入参，金额与配置名前缀误拦已全部消除；这不等于那些旧文案的主观体验已有依据。
- 本轮付费真实重测已启动 #7，待结果后补记；任务 #5/#6 原证据保留，均不改审核状态。

## #7 实测与用户批准的单图续跑

- 本轮类型内抽到新母文 #537《试驾完比亚迪海豹07DM‑i，口碑果然真实！》，不是上轮 #596；仍是 7 条试驾母文候选内的正常选择。政策指纹与 #6 完全一致。
- 文案用时 104.005 秒，工具序列 get → sanitize → validate → validate。第一次仍因简称“405版”不在政策原文字面中报错，第二次模型改为已登记全称通过；老问题的前缀误判/6.39w 等值问题未再出现。不能宣称这一轮完全没有多余修正。
- 产出标题《试驾前看零跑A05，政策真实！》。无依据的主观试驾结论改为观察问题，但仍用“小短板”标题，并把原产品亮点中的部分内容换成政策权益；“不设门槛质保”也不够准确。程序硬校验通过不等于文案可不审核使用，保留本轮原文不手改。
- 首次图片规划在两次尝试后因“缺少逐槽母版文字映射”失败；旧实现没有保存原始模型响应，无法进一步确认是缺字段还是数量不一致。两轮现有外层换模板恢复也失败（没有未使用的同类型母版），没有发送任何生图任务。
- 用户再次明确批准只修图片结构化返回、保留文案和 #324 子方案补图。改为模型只返回 slot_mappings，由代码派生 text_blocks；如果模型另返一份矛盾清单仍报错，不虚构来源映射。保留所有现有金额/配置/OCR校验。报价规划 token 预算调整为 5000，截断响应直接报错，保存每次响应、结束原因、用量及校验明细。
- 增加显式 `--retry-image-plan-keys`，只允许文案已成功但图未完成的条目重试原母版；归档旧规划，不重生文案、不改已成功配图。此次副本另存于 `run-7/before-structured-plan-repair/`；完成回传使用原任务的同 Worker 单向恢复接口，不直接改数据库状态。
- 修复后第一次规划通过：finish_reason=stop，输入2394 / 输出2163 token；只返回 adapted_prompt、slot_mappings、scene_change、vehicle_angle。生图任务 #29993（第一张图）已被线上接受。
- 但该规划把原“四张圆形便利贴分别写报价”变成“五行四列表格”，四张便利贴沦为装饰。原因可见于既有报价指令要求“表头、配置行、价格列”，与这张母版的便签报价结构不匹配。没有把结构化校验通过当成版式复刻通过；本轮不再擅自改文案或重生下一轮。

## 单图续跑完成结果

- 补图开始 11:58:56，完成 12:03:08，共 252.586 秒（约 4 分 13 秒）；仅为本次图片修复续跑耗时，不是整篇总耗时。一次规划、一次线上生图，未重生正文；续跑前后完整文案对象哈希一致。
- 图片任务 #29993 成功，使用原 #324 方案10与对应车型辅助图。金额/配置/OCR硬校验通过，人工逐项查看成图 15 个金额与现政策一致，日期为“9月底前”；但便签报价被改成表格这一版式问题仍在。
- 已通过原 Worker 完成接口回填 Web 任务 #7，状态为待审核，图片和全文在页面可见。#5、#6 保持原结果及待审核状态；没有发布、没有开启定时，队列无运行中或待运行任务。
- 完整结果入口：http://127.0.0.1:3100/workflows/hermes/single?run=7
- 证据目录：`outputs/xhs_hermes_acceptance_20260907/run-7/`，包含原失败副本、续跑日志、`image-repair-summary.json`、原始模型响应与验证记录、交付 JSON/HTML 及图片。
- 测试后已停止临时凭证后端，恢复普通本地后端（lifespan off，未启动调度），删除本次临时 Worker token；历史记录和调试证据保留。

### 尚未解决，不能宣称整篇免审合格

1. 报价通用指令仍强制表头/行/列，导致四张报价便签被改成表格，需要另行授权调整为保留母版承载结构。
2. 正文虽未编造已验证的试驾体验，但“小短板”标题与待验证问题不一致，并向产品亮点段硬加政策权益；“不设门槛质保”也不是准确表述。按本次只修图片返回结构的授权边界，未擅自重写。
3. A05 有效知识仍缺失；405版简称仍可能多一次纠正；本次通过不代表已消除所有误拦或可直接每日免审运行 40 篇。

## 用户同意继续：按原槽位语义和版式修正

- 仅修已指出的文案跨语义填充、标题姿态不一致及报价载体被强制表格化问题。保留 #7，另建 #8 对照测试，不审批、不发布、不启用调度。
- 文案撤掉“任意未重复的已知事实填槽”，明确只换同类信息；产品段不能拿政策、质保凑数。缺依据时改同话题观察问题或删条，联动轻改对应标题，不写死新的全文模板。
- `免费` 不再机械替换为改变适用范围的“不设门槛”，而是删除该营销修饰词，保留首任、非营运等既有条件；局部语法不通顺由 Agent 修正并重新执行原清洗校验。
- 图片撤掉多配置必有表头/行列的指令。只有表格母版才保留表格；便利贴/卡片等维持载体数量、形状、相对位置和原有价格密度，政策登记只作为可选数据池，不强塞全部配置/价格列。最终生图指令也禁止把报价便签改为空白装饰。
- 回归测试 72 项通过。新增测试覆盖权益条件不被扩大、三种不同报价载体的提示词约束、普通非报价母版不新增报价表及最终生图载体要求；这些测试只证明规则传递，不等价于模型视觉遵循已验收。
- #8 是控制变量复测：通过 Web 正常下发，再仅为本次验收复用 #7 的母文 #537、#324 方案10、账号42、当前政策和车型资料快照。正文、规划、图片全部重新运行已有 Worker/Hermes 管线；不改变普通任务随机选库策略。结果待回填。

### #8 已观察到的阶段结果

- 同步当前政策后指纹仍一致；72 项运行器测试及 17 项隔离 Web API 测试通过，共 89 项。
- 文案子进程记录 101.28 秒，原母文未换。标题《试驾前看零跑A05，政策到9月底！》；“小短板”已改“试驾时留意这几点”，质保适用对象保留，没有生成“不设门槛质保”。但仍未遵循按同类槽位替换，不能记为文案验收通过。
- 具体不合格对照：原补能快充条变成充电桩权益；原底盘条变成外饰权益；原智驾条变成配置报价；原安全条变成质保；原座舱条额外塞综合权益。说明增加外层自然语言规则不足以保证逐槽复刻。
- 已读取实际 get_case 工具实现：它仍将完整 case（含所有政策权益）与母文一次返回，没有逐段替换契约；未发现工具另加强制填充指令。插件目录的旧说明还有“unused facts”字样，但本轮禁用了 skills 工具，不能据此断言模型实际加载了那段旧说明。
- 图片规划第一次通过，保留粉/蓝/橙/黄四张圆形便利贴，每张一个配置、两项金额，未塞入第五配置；车型拍立得保留右下方。线上任务 #29994 已提交一次，最终视觉/OCR待核。
- 已请用户确认是否进一步将文案改为“逐段替换清单 + 程序保留未改原句”的生成方式，并仅重测文案、保留此次配图。未获确认前不继续扩大改造或追加生成。

### #8 成图与回填完成

- 任务 #29994 第一张即成功，规划一次、图像一次，无换母版或图片重试。成图确实保留四张粉/蓝/橙/黄圆形报价便签、中央撕纸大标题、右下方汽车拍立得，不再出现另起表格。
- 8 个金额与 4 个配置名均通过 OCR 回读；人工逐项与本次政策行核对一致。条件文字写报废更新按相应条件测算；日期使用“9月底”，没有具体日。没有为了展示全部政策增加第五张配置便签。
- 文案本轮一轮外层生成（内部两次清洗/校验），子进程记录101.28秒；图片一次规划、一次提交。检查点从准备开始12:29:50至图片完成12:36:06，共6分16秒，含准备和调度，不能当作净生图耗时。
- Web任务 #8 回填为待审核，#7保持原样。图片路径：`outputs/xhs_hermes_acceptance_20260907/run-8/2026-09-07/images/42/post-01-a1.png`；完整图文入口：http://127.0.0.1:3100/workflows/hermes/single?run=8 。
- 未批准或发布，整篇仍因前述文案问题未达到人工验收标准；`production_ready` 仅代表现有程序硬校验完成，不代表已通过母文语义复刻质量审核。
- 本轮临时Worker已退出，已恢复普通本地后端并删除临时测试token；后续逐段替换机制等待用户确认，不继续自发改造或追加生成。
