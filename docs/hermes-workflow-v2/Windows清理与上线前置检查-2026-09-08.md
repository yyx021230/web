# Windows 清理与上线前置检查 · 2026-09-08

> 本文按操作顺序追加，早期的“尚未迁移/未获许可”等是当时状态。最新进展见文末。

> 最终状态：2026-09-08 14:56 公网切换已完成，图片地址修复及页面验收通过。
> 详见 [Windows 上线验收](Windows上线验收-2026-09-08.md)。

## 范围和当前边界

用户允许开始上线，随后明确要求：先删除 Windows 不需要的文件再备份，**不备份到 Mac**。
之后用户明确要求 **不要停止新任务**。本轮不冻结提交、不暂停队列、不停止或重启生产服务，
不执行 Docker/WSL 离线回收，也不执行会中断服务的上线切换。之前提出的停服维护未获授权。
截至此记录，线上仍为 0.3.18，未切换镜像、迁移生产库、停止服务、开启 Hermes 定时或发布。
本页仅保存检查与操作元数据，不是生产数据备份。

## 已完成清理

- 删除 13 个已核对未被任何容器引用的旧应用镜像：
  backend 0.3.8 / .9 / .10 / .11 / .12 / .14 / .15 / .16；
  frontend 0.3.8 / .9 / .10 / .14 / .15。
- 保留当前 0.3.18、上一版 0.3.17、既有 0.3.19 候选、两个 Hermes 候选、基础镜像。
  未执行 `system prune`、volume prune 或删除业务数据卷。
- 清理超过 72 小时未使用的构建缓存，Docker 报告回收 **4.177 GB**。
  缓存可重建；删除的旧镜像需由对应源码及保留的发布包重新构建。
- 对 D 盘历史 incoming 包和 C 盘正式 packages 包逐一核对 SHA-256 后，
  删除 10 个完全重复的 D 盘文件，共 **7,176,997,350 字节**。
  C 盘保留副本未删除，因此这些重复包可恢复。
- 精确文件清单位于 Windows：
  `D:\ztqc-release-archive\cleanup-20260908-duplicate-packages.json`，含保留位置和 SHA-256。

清理重复包后 D 盘可用 **63,398,240,256 字节**；C 盘可用约 19.4 GB。
站点 HTTP 200，六个生产容器启动时长未变。

## 完整备份空间问题

- `C:\projects\web\uploads` 是指向 `D:\projects\web\uploads` 的 Junction，
  不是可以删除的第二份数据。
- 上传目录约 **74,281,913,205 字节 / 59,729 个文件**，主要是已压缩 PNG、WebP、JPEG。
- 现有数据库备份的 manifest 均未包含 uploads。每日数据库备份计划当前为 Disabled；
  本次不擅自启用或更改任何现有计划任务。
- 不能把 `D:\ai_ztqc\don_tu` 的约 36.5 GB 原始图片当缓存删；同样保留账号浏览器资料、
  所有业务数据卷、已有数据库备份、系统恢复文件及不明确的旧源文件。
- Docker 删除释放了虚拟盘内部空间，但 NTFS 文件
  `D:\DockerData\Docker\wsl\disk\docker_data.vhdx` 仍为 **62,301,143,040 字节**，
  且未设为 sparse；内部文件系统现用约 **26,461,433,856 字节**。
  不能把内部空闲量当作 Windows 已获得的可用量，也不保证压实能回收全部差额。
- 回收虚拟盘空闲空间需要独立维护安排。按微软的 compact vdisk 说明，磁盘必须卸载或
  只读挂载；本次没有关闭 Docker/WSL、卸载或压实生产虚拟盘。
  参考：https://learn.microsoft.com/windows-server/administration/windows-commands/compact-vdisk

## 上线准备进度（不是生产验收通过）

- 已确认生产容器内 200 个 Python/配置文件与发布提交 `7b8c109` 一致；生产 MCP 二进制
  SHA-256 为 `9bce522cd69b56b0bd405b03592d4885f90b016203ca097ede771e2a0ea534e0`，
  不重新构建或替换这个无关组件。
- 创建隔离候选工作树：`/private/tmp/hermes-web-release-20260908.6FmqFJ/release`，
  分支 `codex/hermes-windows-0.3.20`，以线上 0.3.18 为底，仅合入 Hermes 模块。
  尚未提交/打发布标签，也未完成 Compose 整合、正式备份恢复演练或正式镜像构建。
- 前端 57 项测试通过；纳入现有覆盖范围的总体语句/行 95.2%、分支 83.82%、函数 83.05%；
  TypeScript 和 Next 生产构建通过，生成 38 个路由。
- Alembic 单头检查为 `c3d4e5f6a7b8`。隔离候选修正了迁移测试仍指向上一迁移的断言；
  尚未把此测试修正覆盖回实际开发目录。
- 后端全量测试运行被用户打断，不能认定全量通过；缓存中记录
  `test_ai_queue_status_requires_admin` 在全量上下文中失败。
  单独重跑所在安全/提示词组 13 项全部通过，仍需诊断顺序依赖并重跑全量，不能忽略。
- 生产 `ai_tasks` 约 319.9 MB，7 个终态记录的 params 超过 1 MiB；本次未改写它们，
  未做会锁表的 VACUUM FULL。旧门禁中的历史大字段清理需单独评估，不照搬 19 GB 时期结论。

## 本次数据库备份结果

- Windows 目录：`D:\ztqc-backups\web\20260908_110810`。
- `postgres.dump` 为 **846,224,628 字节**；`pg_dump` 和 `pg_restore -l` 均成功退出。
- SHA-256：`8557e157f7b46b0c1d3bfc3703b80b11802215bec2f6eb729922fbdd75235271`。
- manifest 时间：`2026-09-08T03:10:04.5752740Z`。
- 自动保留期删除关闭，外部镜像复制关闭；备份文件没有传到 Mac。
- 这是数据库逻辑备份及目录校验，**尚未做隔离数据库完整恢复演练**。
- `uploadsArchive=null`：完整 uploads 备份仍未启动，不能称为完整生产备份。
- 数据库备份后 D 盘可用约 62.55 GB。若后续获准维护，先排空任务、优雅停服务，
  再按官方流程回收虚拟盘空闲空间；恢复服务后以实际可用空间决定是否满足全量备份余量。
  不为凑空间删除任何原始图文或系统恢复文件，也不保证虚拟盘全部理论空闲都能回收。

## 保持接单后的检查

- 再次检查时六个生产容器启动时长未变，D 盘可用 **62,432,866,304 字节**。
- 余下 `D:\projects\web-archive` 含历史数据库导出和旧源码包；不能只凭目录名认定无用，
  本轮未删除。剩余历史 incoming 包未确认有相同保留副本，也未删除。
- 本轮仅只读检查和更新本记录；没有新的远程删除或备份传输。
- 完整上传文件备份仍受空间限制，尚未完成。保留已验证目录的数据库备份，
  不在剩余空间不足时强行打包，也不将“不停新任务”理解为允许降低备份/上线门禁。

## 用户后续确认：本次跳过图片全量备份

用户在“保留数据库备份、本次跳过图片全量备份”的明确询问后回复“继续”。
因此本次发布不再以 74 GB 图片全量备份作为前置条件；这不是已有图片备份完成的声明。
数据库备份仍保存在 Windows，实际图片目录、Junction 和所有数据卷保持原位，不复制到 Mac。
线上持续写入时，切换前还需确认数据库快照时点；不得用旧数据库备份覆盖新产生的任务。

## 同一公网地址切换的检查结果

- 公网 `47.98.127.132:18080` 通过 FRP 转到 Windows `127.0.0.1:3000`。
- 现有 `C:\ProgramData\frp\frpc.toml` 未开启 `webServer`；只读执行 `frpc status`
  明确返回 `web server port should be set if you want to use this feature`。
- FRP 官方说明：动态更新代理需要启用 webServer；非代理的公共参数只能在启动时生效。
  因此当前配置不能直接使用 reload 完成无中断入口切换。
  参考：https://gofrp.org/en/docs/features/common/client/
- 原 `Deploy-Release.ps1` 会停止 frontend 接单并等待队列清空，随后停止 API/AI Worker，
  不符合用户“不停新任务”的边界，本次未执行。
- 新旧应用并行验证与最后公网入口切换是两步。后者若需短暂重启入口，会造成网页访问、
  公网 API 请求短暂中断，不能描述成零停机；必须先取得用户许可。
  现有 API、AI Worker、数据库、Redis 不应随入口一起停止。
- 本轮复查站点 HTTP 200，六个生产容器启动时长未变；D 盘可用 62,278,078,464 字节。
  尚未启动新版 Web 候选、迁移生产库、重启 FRP 或开启 Hermes 定时生产。

## 全量后端测试问题定位

- 可重复失败：334 项通过后，权限测试 `test_ai_queue_status_requires_admin` 的管理员分支
  因 Redis 连接关联前一个测试已关闭的事件循环报错（different loop / Event loop is closed）。
  未登录 401、普通用户 403 已实际通过，不是权限放行错误。
- 隔离候选仅把该权限测试的队列状态读取替换为 AsyncMock，增加“未授权不调用队列、
  管理员恰好调用一次并返回数据”断言；没有修改生产权限依赖、队列实现或错误拦截。
  Redis 队列行为仍由独立队列单元测试覆盖。
- 隔离候选全量重跑：**712 passed，124.62 秒**；没有跳过原失败测试。
  唯一 warning 是 Mac Python 3.9 使用 LibreSSL 的 urllib3 提示，不是 Windows 运行时验收。
  报告：`/private/tmp/hermes-web-release-20260908.6FmqFJ/backend-full.xml`。
- 权限测试隔离修改和迁移测试 head 修正已同步到实际项目 `/Users/yyx/ztqc/web`；
  两个测试文件的 Ruff 检查、候选 `git diff --check` 均通过。没有改生产权限或队列代码。
- 以上为候选代码回归结果，不代表正式 Web 镜像已构建、数据库恢复演练已完成或生产上线已验收。

## 确认入口切换后的推进与新阻塞

用户已明确确认最终入口可以短暂不可用，但现有生产进程和队列不停止。

- 采用保留原 API / AI Worker / 前端的并行部署：新网页使用 Windows 回环 3101，
  新工作流 API 使用回环 8001；最终只把既有 FRP 入口从 3000 切到 3101。
  其他业务 API、认证和图片继续访问原后台，不重复启动原有调度或大报表预热。
  Hermes 单独使用 `hermes-content-scheduler` 租约，初始生产计划依然关闭。
- 候选分支提交：应用与模块 `c6e6a3350b948f43292f40145b4bf8c7f73c421b`，
  最终并行入口配置 `5bf479400f0891d8f762f5b081bfa2638069bf30`。
  实际项目尚未覆盖全部并行发布文件；候选完整源码在前述隔离工作树。
- 新增两项生命周期测试后，全量 **714 passed / 150.80 秒**，应用分支覆盖口径总覆盖率
  **70.37%**；前端 57 项、覆盖率门槛、类型检查、38 路由生产构建均通过。
- 源包已传到 Windows `D:\ztqc-hermes-release\0.3.20`；仅源码/静态资产，非业务备份。
  主包 SHA-256 `a75cd92fcfca99492c0129bd1c23ace75ef62804bfef3abdae3e5afc0be95cac`；
  最终配置补包 SHA-256 `6679714537e91c0b6b9b7a6e536a83d65d356ff347ce852c3120e9b0331e3e49`。
- Windows 已构建 `ztqc/hermes-api:0.3.20`；Worker 已验证并重标记 `ztqc/hermes-worker:0.3.20`。
  运行凭据只写在 Windows 受限目录，未输出值；没有启动正式 Hermes API/Worker。
- 在独立容器 `web-hermes-restore-20260908`、专属临时卷 `web_hermes_restore_20260908`
  和数据库 `hermes_restore` 成功完整恢复备份，未连接生产网络、没有开放端口。
  副本恢复后大小 **3,577,347,431 字节**，40 用户、30,090 生图任务、178 账户环境。
- **PostgreSQL 副本迁移演练失败，正式迁移因此未执行**：迁移 `c3d4e5f6a7b8` 的外键名
  `fk_hermes_workflow_posts_publish_target_environment_id_xhs_environments`
  超出 PostgreSQL 63 字符上限。SQLite 回归不会发现这个限制；不能把其通过作为 PG 通过。
- 已复核副本事务回滚：head 仍为 `a1b2c3d4e5f6`，Hermes 表为 0；生产同样为旧 head / 0 表。
  原六个生产容器未重启，5 个真实生图任务仍在处理，公网 HTTP 200。
- 下一步仅需修正数据库约束命名的跨数据库兼容性，并补 PG 方言回归、重新构建 API、
  在已恢复副本重测。依照用户“遇到问题先询问”的要求，此修正尚未擅自执行。
  未开启日程、未创建真实 Hermes 生图、未更改 FRP 配置、未切换公网。

## 获准兼容修正后：候选启动、正式迁移及真实验收

- 约束命名修正获准并完成：创建/回退统一 `op.f(...)`，真实 PostgreSQL 副本
  完整升级、降级、再升级均通过；实际外键名以 `_46d5` 收尾、长度 60。
  副本用户/账号/文案/提示词数量不变，验证后仅停止临时恢复容器以释放资源，保留其卷。
- 最终后端全量 **716 passed / 127.12 秒**；额外 PG/SQLite 命名与升降级 3 项通过。
  后端修正提交 `98dcb137daaf04994266a07189be0863f3bedb0e`；镜像直启配置提交
  `92fd6aa23f69969c1992f00bd47268604113b77e`。相关部署文件已同步实际开发项目。
- 新鲜生产库备份保存在 Windows `D:\ztqc-backups\web\20260908_143009`，
  大小 **844,143,190 字节**，SHA-256
  `b084eae747b5c02c342c1b65cd5a982cb3728b23156ecbf51de36ee3eb5a0a17`。
  dump / 目录校验成功；此前同 schema 备份已完成恢复演练。仍未包含图片全量备份。
- 生产库正式升级至 `c3d4e5f6a7b8` 成功：用户 40、账号环境 178、文案 2210、
  提示词 576、项目 0，迁移前后相同。只新增 Hermes 表和字段。
- Windows 新版 API、网页和 Worker 均已 healthy。API `/health/ready` 数据库、Redis、
  存储均正常；存储挂载与旧 API 完全一致。原六容器未重启。
- 网页初次构建遇到一次 Docker RPC EOF；没有证据归因为 OOM。原服务恢复正常响应，
  临时演练库停止后串行重试构建成功；没有重启 Docker/WSL 或生产容器。
- 候选网页实际登录成功；工作流广场、批量记录可见。读验收覆盖 11 款政策、
  2054 条可用母文 / 525 条母图、7 种文案类型及 7 种图片类型实例。
  库可用数与数据库总数口径不同，不是本次迁移删减数据。
- 真实验收只下发 **1 篇**：任务 #1，环境 42（云先派懂车社）、负责人用户 3、零跑 A05。
  Windows Worker 已接单；没有新增 40 篇任务。完整结果尚待验收。
- 每日 8×5 计划仍关闭；真实发布未启用。公网 FRP 在此时仍指向旧网页 3000，
  未完成最终入口切换。Windows 验收元数据在 `D:\ztqc-hermes-release\0.3.20`。

## 真实任务完成后的浏览器验收阻塞

- 任务 #1 在北京时间 **14:40:29—14:45:17** 完成，运行约 **288.49 秒（4 分 49 秒）**，
  文案 114.15 秒、提示词规划 34.329 秒、生图及 OCR 136.934 秒。
  生图任务 `30403` 为 completed，仅提交一次；没有人工改文、换母版或额外重生。
  母文 #278、母图 #216，图文程序硬校验通过，状态为 `review_pending`，尚未人工审核/发布。
- 线上真实读取环境 42 的最近 15 条完整帖子，缺正文 0 条；比对 15 条正文，
  4-gram Jaccard 分数 0.0087、阈值 0.85。该值不是语义相似百分比。
  最新已同步帖为 2026-07-30；只能证明读取当前库内最近 15 条，不声称平台数据实时。
- 浏览器验收发现 **成图地址暴露 Docker 服务名**：返回的 `image_url` 和
  `source_detail.selected_prompt_image` 以 `http://backend:8000/uploads/` 开头。
  Worker 内部能下载，但用户浏览器无法解析 `backend`，卡片实际显示“图片暂时无法加载”。
- 同一成图通过候选站点 `/uploads/ai-images/b9e1a22dcdb24454972541543c0a0243.png`
  实测 HTTP 200、PNG **1,804,497 字节 / 1086×1448**，文件和存储挂载正常。
  这是内部下载 URL 与对外展示 URL 未分离，不是生图失败、文件丢失或需要重新生成。
- 待用户批准的最小修复：仅在工作流 API 对外序列化时，将已知内部存储图片 URL
  映射为同源 `/uploads/...`；同时处理母图预览，保留外部车型图 URL 和 Worker 内部下载地址。
  不修改成品/提示词/数据库历史值，不重启原 API 或原 AI Worker。
  需重建并重新加载新工作流 API，然后重测卡片及原图再切换入口。
- 按用户“有新问题先询问”要求，尚未执行该新修复。FRP 仍为 3000，公网尚未升级。
  切换脚本未执行，已增加对外图片路径门禁，避免只按 `hard_pass` 宣称上线验收完成。
  已知创建时间少显示 8 小时问题延续前轮记录，本轮未擅自改动时区或历史时间。
