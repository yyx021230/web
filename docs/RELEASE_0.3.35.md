# v0.3.35 首页相关推荐分类边界修复

更新日期：2026-09-15

`v0.3.34` 上线实测发现，通用 JSON 提示词会把当前首页中关键词相似但分类不同的素材带入相关推荐。本版本将推荐候选严格限制在当前素材的完整分类池中，再在分类内部执行语义排序和去重。

发布范围只有主前端和 Hermes 前端；核心后端、AI 生图 Worker、Hermes API、Hermes Worker 和数据服务均不得重启。
