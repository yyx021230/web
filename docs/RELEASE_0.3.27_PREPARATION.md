# v0.3.27 上线准备记录

更新日期：2026-09-14

## 版本边界

- 核心候选版本：`0.3.27`；`v0.3.26` 已发布为不可变候选，不移动、不覆盖。
- 生产核心回执：`0.3.22 / a1a906340d2e`。
- 候选版必须包含 `v0.3.23` 的投流聚合内存上限修复和 `v0.3.26` 的完整功能范围。
- 唯一核心发布入口仍为 `WIN_PASS=... ./deploy-to-windows.sh`，禁止手工解压源码重建。

## 本次范围

- AI 生图：GPT Image 2.5 模型池、模式选择、失败任务参数回填、创作区布局与状态交互。
- 应用外壳：固定侧栏、站内登录弹窗、品牌图标、响应式工作区。
- 首页：提示词宝库改为首页，来源筛选、随机首屏、瀑布流与详情复用。
- 素材：图库、文案、工作台、发布页视觉与交互调整。
- 功能退役：模板库、编辑器、项目入口及其静态模板资源完整移除。
- 数据：提示词来源元数据迁移，保留现有内部素材并增量安装 1000 条开放授权外部素材。
- 稳定性：发布队列按实际排队量启动并发 worker，保留同环境互斥。

## 数据与持久化

- 发布包不包含 `.env`、数据库、`uploads` 或本地 `dev.db`，不会覆盖生产数据。
- 生产 PostgreSQL 当前迁移为 `c3d4e5f6a7b8`，目标 head 为 `b36e8d1f4a2c`。
- 迁移顺序：`a25f7c9e1d3b`（Team GPT Image 2.5）后接 `b36e8d1f4a2c`（提示词来源字段）。
- 来源字段迁移默认值为 `internal`，上线前已有提示词会统一归为“内部”。
- 生产 `/app/uploads` 当前绑定到 `C:\projects\web\uploads`，发布前后必须保持不变。
- 1000 条外部首页素材作为独立数据包传输，不进入 Git 发布包；安装前校验 SHA256 和全部引用图片。
- 素材安装必须使用增量模式，不得传入 `--replace`；重复执行只更新同一批外部 ID，不重复新增，也不软删除内部素材。

## 素材包

- 本地文件：`/tmp/prompt-homepage-catalog-open-catalog-20260914.tar.gz`
- Windows 预上传文件：`C:\projects\web\.release\incoming\prompt-homepage-catalog-open-catalog-20260914.tar.gz`
- SHA256：`a5c5b825381d5a66caf3cad9a36af04a09e86516d875875da05b2837cfb0adf4`
- 清单：包内 `prompts/catalog/open-catalog-20260914/catalog.json`
- 安装脚本：`scripts/windows/Install-PromptHomepageCatalog.ps1`
- 外部数据：MeiGen 开放提示词 900 条、DiffusionDB 100 条；提示词、图片路径与外部 ID 均唯一。

## 发布前状态

- 生产服务当前健康运行，发布锁可用。
- 最近一次检查时生产有效提示词为 639 条；它们均须保留并归为“内部”。
- 素材安装后的预期外部数量为 1000；若发布期间内部数量未变化，总数预计为 1639。
- 安装器不硬编码总数：它会验证内部数量前后不变、外部数量恰好为 1000、外部来源分布为 900/100。
- 正式切换必须等待 AI 生图、Dify、小红书同步、报表和发布队列排空。

## 正式上线顺序

1. 确认 AI、生图、Dify、小红书同步、报表、发布队列全部为 0。
2. 只清理无引用构建缓存与旧候选文件，禁止清理当前回执镜像、数据库卷和 uploads。
3. 通过唯一发布入口上传、构建并验证 `v0.3.27` 核心候选版。
4. 确认 `/version`、`/health/ready`、发布回执、镜像身份、PostgreSQL 和 uploads 挂载全部正确。
5. 执行带 SHA256 的素材安装脚本，以增量模式加入 1000 条外部素材。
6. 验证原内部素材数量不变、外部素材 1000 条、来源分布 900/100、图片 HTTP 200，并抽查“全部/外部/内部”、详情页与“使用创意”回填。
7. 走登录、生图、任务中心、图库、文案、工作台、发布、小红书同步、运营看板、投流看板全链路冒烟。

## 回滚

- 核心发布失败由 `Deploy-Release.ps1` 自动恢复上一不可变镜像和发布前数据库备份。
- 素材导入在数据库单事务中执行；安装前校验失败不会改数据库。
- 素材安装后若业务验收失败，使用本次核心发布记录的发布前 PostgreSQL 备份恢复；新增图片目录可保留，不影响旧数据引用。
