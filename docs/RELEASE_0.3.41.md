# v0.3.41 生图队列与工作流发布

## 范围

- 生图异步提交返回 202；前台任务与 Hermes 批量任务使用独立队列。
- 生成并发 18 + 12，去水印独立 12 路；保留任务 ID 恢复、取消、历史隔离。
- 模型入口按容量与健康状况分流；前端任务状态、参考图与重试参数回填。
- Hermes 创作梯度、批次差异化、每周模板使用降权，任务并发 10、文案/图片各 3。
- 首页素材筛选与展示调整。不执行任何素材导入或删除脚本。

## 发布前检查

1. 保留线上 PostgreSQL、Redis、MinIO 与 uploads 挂载，禁止上传本地数据库或环境文件。
2. 发布包只从已提交并打标签的 Git 导出；不包含 exports、车型抓取脚本与本地缓存。
3. D 盘需要 20GB 可用空间；历史构建缓存必须先离机归档并逐文件校验，不删用户图片及数据库备份。
4. 先构建候选镜像，再检查所有在途生图、去水印、小红书与 Hermes 任务，排空后切换。
5. 在线迁移 b36e8d1f4a2c -> c47f2a6d9b10，新增可空的历史隐藏字段与索引。
6. 保留独立 Hermes API 路由。核心前端旧版本 override 必须解除，避免下次启动回退。
7. 独立 Hermes API 负责其定时器，核心设置 HERMES_SCHEDULER_ENABLED=false，避免两组租约重复调度。
8. 核心和 Hermes 分别发布；Hermes Worker 必须包含全部 Python 模块，不能只复制两个入口文件。

## 恢复与回滚

- 新增 ai-postprocess-worker 纳入回执、启动恢复及健康监测。
- 首次发布回滚时停止新去水印容器；旧版本无法消费 postprocessing，必须先排空任务。
- 保留旧镜像、发布包、源码与配置快照；发布备份不顺带清理历史数据库备份。
- 核心前端和后端版本统一，解决独立前端更新后恢复脚本版本校验不一致的问题。

## 验收

上线前已重新执行完整后端 777 项、前端 105 项、Hermes 432 项及 TypeScript 检查。
线上验收以实际回执为准，要求普通生图、参考图生图、刷新恢复、批量队列、去水印、
结果图片访问及 Hermes 完整生成链路验证；页面 200 不代表业务验收完成。
