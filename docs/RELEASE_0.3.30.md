# v0.3.30 发布预检修正

更新日期：2026-09-14

`v0.3.29` 的 Hermes Worker 发布在构建和服务切换前被 PowerShell 预检中止，未部署到生产。原因是缺失候选镜像属于预期状态，但 `docker image inspect` 在全局 Stop 模式下被解释为终止错误。

`v0.3.30` 使用无错误的镜像列表查询完成不可变标签冲突检查，并完整承载 `v0.3.29` 的首页与工作台修复。数据库、线上数据和运行容器未受失败预检影响。
