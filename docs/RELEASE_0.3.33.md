# v0.3.33 首页补丁验收标记修复

更新日期：2026-09-14

`v0.3.32` 候选前端构建和页面启动成功，但上线脚本使用的连续中文文本在 Next.js 压缩后不存在，因此验收失败并自动恢复 `v0.3.31`。本版本保留相同的详情图片适配与跨页去重修复，仅把发布验收改为检查候选编译产物中稳定存在的去重函数标记。

发布范围仍只有主前端和 Hermes 前端；核心后端、AI 生图 Worker、Hermes API、Hermes Worker和数据服务均不得重启。
