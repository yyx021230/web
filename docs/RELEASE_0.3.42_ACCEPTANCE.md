# v0.3.42 生产发布验收

日期：2026-09-20（北京时间）。业务发布提交：`109b12e9feab`，标签：`v0.3.42`。

## 发布结果

- 核心前端、API、生图 Worker、独立去水印 Worker 与 Hermes API/Worker 均已切换至 0.3.42。
- 核心发布包 SHA256：`b519fcd2042e89ffdc5ebad13faf61f024ef996c1580796c90ab4d688de0c412`。
- 核心回执：`C:\projects\web\.release\current.json`；Hermes 回执：`C:\projects\web\.release\hermes\current.json`。
- `Assert-ReleaseState.ps1` 再次执行 `verified=true`，版本、镜像、源码指纹、MCP 指纹和去水印 Worker 检查全部通过。
- 生产备份：`D:\ztqc-backups\web\20260920_093623\postgres.dump`，1,033,891,442 字节；已校验归档可读。
- 数据库迁移 `b36e8d1f4a2c -> c47f2a6d9b10`。生产数据库与 uploads 挂载保持不变，没有上传本地数据库或环境文件。
- 原在途 Hermes 84 等待 10/10 篇完成后才切换；没有取消用户任务。
- 外网探测记录到切换窗口 09:36:17 至 09:39:04，约 167 秒，主要是数据库压缩备份。后续 Hermes 切换没有重启核心生图服务。
- 代码分支与发布标签均已推送到 origin。0.3.41 的预检失败保留原标签，未复用或覆盖。

## 真实业务测试

不是仅检查 HTTP 200；以下任务实际调用生产模型并生成图片，均完成去水印与结果落库。

| 测试 | 任务 | 结果 | 提交耗时 | 后端生成及处理耗时 |
| --- | --- | --- | --- | --- |
| GPT Image 2 文生图 | 33702 | completed；PNG 可完整解码 | 0.098 秒 | 111.60 秒 |
| GPT Image 2.5 快速文生图 | 33703 | completed；PNG 可完整解码 | 0.089 秒 | 51.97 秒 |
| Seedream 批量队列 | 33704 | completed；JPEG 可完整解码 | 0.099 秒 | 32.74 秒 |
| GPT Image 2.5 精细参考图生成 | 33710 | completed；PNG 可完整解码 | 0.100 秒 | 87.77 秒 |

- 四条提交均返回 202；重复同一个 client_request_id 返回原任务 ID；按请求 ID 找回任务成功。
- Hermes 85：测试4，零跑A05，replica。Hermes 86：测试5，零跑C10，interpretive。由两个不同 Worker 槽同时执行。
- 两条 Hermes 均进入 review_pending，帖子 439、440 有标题、正文、图片且 hard_pass=true。两张图片均能完整解码为 1536×2048，外网图片地址返回 200。
- 两条 Hermes 使用内部提示词库（642 条）与文案库（2054 条），批量生图任务分别为 33709、33711，均 completed。
- 测试仅生成待审核内容，没有执行审核通过、发布排期或发布到小红书。
- 运行参数核对：实时生图 18、批量生图 12、去水印 12；Hermes 10 个在线槽，单任务文字与图片各 3。
- 7 个主要页面返回 200，关联的 25 个 JavaScript 资源全部返回 200。
- 上线前已执行后端 777 项、前端 105 项、Hermes 432 项测试与 TypeScript 检查；候选镜像生产构建完成 34 个页面。

## 边界与待办

- 本次没有开展满 30 路的生产压测，不能用少量成功样本承诺满载吞吐或所有上游路径都稳定。
- 浏览器自动化连接在验收期间两次超时，未完成上线后的完整页面交互回归；上面页面与脚本检查属于 HTTP 检查，不冒充浏览器点击验收。
- 外网原图传输仍偏慢。同一张 1,691,958 字节 PNG：内网 0.215 秒，Windows 主机通过公网完整读取 32.69 秒，本机一次公网请求 20 秒只收到 1,266,310 字节。应用接口和图片存在性正常，但不能据此断言外网传输已优化完成；需要继续区分带宽与 FRP 链路瓶颈。
- 本机探测在 09:40 至 09:55 出现间歇超时，同时本机负载显著异常（load average 超过 150）；生产容器没有重启、服务器内网健康检查正常。该区间不能直接当作生产服务持续宕机，也不能忽略为完全正常。
- 最后空间核对：C 盘余 16.59 GiB，D 盘余 23.76 GiB。健康监测仍按使用比例报警，需要后续扩容或经确认后处理历史归档；不再删除项目数据。
- 验收过程记录：`/tmp/ztqc-0.3.42-probes.json`、`/tmp/ztqc-deploy-0.3.42.log`、`/tmp/ztqc-hermes-deploy-0.3.42.log`。
