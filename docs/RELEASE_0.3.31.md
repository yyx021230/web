# v0.3.31 创作前端与工作流 API 隔离发布

更新日期：2026-09-14

本版本只替换主前端、Hermes 前端和 Hermes API，发布首页瀑布流稳定、工作流用户隔离及终态任务删除能力。Hermes Worker 沿用 `v0.3.30`，核心后端、AI 生图 Worker、数据库、Redis 和 MinIO 均保持原容器不动。

发布脚本在切换前确认工作流没有运行中任务，并在切换后逐一核对未更新容器的 ID、镜像和启动时间。候选镜像预先构建完成，最终只进行目标服务的短暂切换；失败时恢复原 Compose 配置和原镜像。
